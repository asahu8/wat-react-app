import React from 'react'
import './style.scss';

const WatLoader  = (props: any) => {
  return (
    <div className="preloader">
      <div className="ui active loader" />
    </div>
   )
 }

export default WatLoader;